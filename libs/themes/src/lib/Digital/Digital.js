import React from 'react'
import './variables.scss'
import './../../styles/main.scss'

export const Digital = ({ className, children }) => {
  return <div className={className}>{children}</div>
}
export default Digital
