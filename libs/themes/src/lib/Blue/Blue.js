import React from 'react'
import './variables.scss'
import './../../styles/main.scss'

export const Blue = ({ className, children }) => {
  return <div className={className}>{children}</div>
}
export default Blue
